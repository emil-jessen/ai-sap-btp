{
  "name": "my-btp-app",
  "version": "1.0.0",
  "description": "SAP BTP application with CAP backend and SAP UI5 Fiori frontend",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_ORG/my-btp-app.git"
  },
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build --production",
    "test": "jest --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "@sap-cloud-sdk/connectivity": "^4.6.0",
    "@sap-cloud-sdk/http-client": "^4.6.0",
    "@sap/cds": "^8",
    "@sap/xssec": "^4",
    "express": "^4",
    "passport": "^0"
  },
  "devDependencies": {
    "@sap/cds-dk": "^8",
    "@sap/eslint-plugin-cds": "^3",
    "eslint": "^9",
    "jest": "^29",
    "mbt": "^1"
  },
  "cds": {
    "requires": {
      "auth": {
        "kind": "xsuaa"
      },
      "destinations": {
        "kind": "destinations"
      }
    },
    "features": {
      "fetch_csrf": true
    },
    "server": {
      "body_parser": {
        "limit": "5mb"
      }
    }
  }
}
